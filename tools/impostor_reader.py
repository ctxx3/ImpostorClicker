# file = open("D:/Projects/ImpostorClicker/assets/impostors.data","r",encoding="utf-8").read()

# def xor_encrypt(text, key):
#     encrypted_text = ""
#     for char in range(len(text)):
#         encrypted_char = chr(ord(text[char]) ^ ord(key[char % (len(key))]))
#         encrypted_text += encrypted_char
#     return encrypted_text
# open("tools/impostors.json", "w+").write(xor_encrypt(file, "susImpostor"))

#print([x for x in range(1, 1000) if not any([x % y == 0 for y in range(2, x)])])
